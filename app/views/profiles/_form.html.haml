%h2 Set Profile
  
%div
  = form_for @profile, url: user_profile_path(@user) do |f|
    %div
      = f.label :bio, "Biography"
      = f.text_area :bio, placeholder: "Enter a short description about you and your powder skills"

    %div
      = f.label :profile_picture, "Profile Picture"
      = f.file_field :profile_picture

    %div
      = f.label :prefered, "How do you ride?"   
      = f.select(:prefered, options_for_select(['Ski', 'Snowboard', 'Both']))

    %div
      = f.label :level, "What's your current level?"      
      = f.select(:level, options_for_select(['Beginner', 'Intermediate', 'Advanced', 'Austrian']))

    %div
      = f.label :specialities, "Do you have any preferences or specialities?"  
      = f.select(:specialities, options_for_select(['Off-Piste Adventure', 'Ski-Touring', 'Racing', 'Cross-Hill', 'None', ]))

    %div
      = f.label :favorite_resorts, "List your favorite resorts"
      = f.collection_select(:favorite_resorts, Resort.all, :id, :name)

    %div
      = f.label :state, "Are you teaching or guiding others in the mountains?"      
      = f.select(:state, options_for_select(['Skiing Instructor', 'Snowboard Instructor', 'Mountain Guide', 'None']))

    %div
      = f.submit "Save"