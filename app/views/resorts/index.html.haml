= javascript_tag do
  window.resorts = #{raw @all_resorts.to_json}

.map-show
  = render :partial => 'map'

%p 
  - @resorts.each do |resort|
    %p 
      = link_to resort.name, resort_path(resort)
  
  = paginate @resorts