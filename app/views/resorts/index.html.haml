#resort_index     
  %ul 
    - @resorts.each do |resort|
      %li 
        = image_tag(resort.map) if resort.map
        %h2
          = link_to resort.name, resort_path(resort)

    
    = paginate @resorts