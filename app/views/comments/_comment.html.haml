.pure-g
  .pure-u-1-3
    %a{href: user_profile_path(comment.user)}
      .profile_pic
        - if comment.user.profile.profile_picture.thumb
          %img{src: comment.user.profile.profile_picture.thumb}
    .cmnt_text
      %a{href: user_profile_path(comment.user)}
        %p
          @
          = comment.user.name
      %p= comment.comment
  .pure-u-1-3 
    - if comment.user != current_user
      - if comment.flagged?
        %p Flagged!
      -else
        = link_to 'Flag', inappropriate_resort_comment_path(Resort.find(params[:id]), comment) 
      